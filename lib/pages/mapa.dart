import 'package:flutter/material.dart';

class MapaPg extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('MapaPg'),
     ),
   );
  }
}
